#include "main.h"
/**
 * _islower - returns 1 if c is lowercase
 * @c : character for lowercased
 *
 * returns 0: Otherwise
 */
int _islower(int c)
{
	if (c > 96 && c <123)
		return (1);
	else
		return (0);
}
