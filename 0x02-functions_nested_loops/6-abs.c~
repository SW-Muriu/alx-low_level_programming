#include "main.h"
/**
 * _abs - edit the absolute value of an integer
 * @c: int type 
 *
 * Return: absolute value of @c
 */
int _abs(int n)
{
	if (c < 0)
	{
		return (c * -1);
	}
	else
	{
		return (c);
	}
}
